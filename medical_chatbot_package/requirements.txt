#의료 챗봇 모델 - 백엔드 연동용 의존성

# 필수 패키지 (데이터 처리)
pandas>=1.5.0
requests>=2.25.0
python-dateutil>=2.8.0

# IBM watsonx (선택사항 - API 키 있을 때만 필요)
ibm-watson>=7.0.0

# 유틸리티
python-dotenv>=0.19.0

# 개발/테스트용 (선택사항)
# streamlit>=1.28.0  # 모델 개발자만 필요

# 백엔드에서 추가할 수 있는 패키지들
# fastapi>=0.68.0
# uvicorn>=0.15.0  
# flask>=2.0.0
# gunicorn>=20.0.0